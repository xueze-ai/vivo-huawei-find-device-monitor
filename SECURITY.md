# 安全说明

## 报告安全问题

请不要在公开 issue 中提交账号、密码、验证码、SendKey、Cookie、设备 ID、精确坐标、家庭成员姓名或带位置的截图。

如果发现代码层面的安全问题，请只提供能够复现问题的最小化脱敏信息。若 GitHub 仓库已启用私密漏洞报告，请优先使用该入口。

## 支持范围

项目会尽力避免在日志中输出凭据，并默认将 noVNC 绑定到回环地址。部署者仍需负责服务器更新、SSH 安全、文件权限、秘密轮换和设备所有者授权。
